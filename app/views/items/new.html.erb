新しく持ちものを追加する
<%= form_with(model: @item, local: true) do |f| %>
名前
  <%= f.text_field :name %>
季節
  <%= f.select :season, ['春', '夏', '秋', '冬','通年'], {include_blank: '選択してください'} %>
移動方法
  <%= f.text_field :toransportation %>
    <%= f.collection_select :relations, Activity.all, :id, :name ,:include_blank => "選択してください" %>
   <%= f.submit %>
<% end %>